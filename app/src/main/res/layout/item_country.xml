<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/background_item_list"
    android:clickable="true"
    android:orientation="horizontal"
    android:padding="@dimen/item_country_picker_padding">

    <ImageView
        android:id="@+id/iv_country"
        android:layout_width="@dimen/item_country_picker_width"
        android:layout_height="@dimen/item_country_picker_height"
        android:layout_gravity="center"
        android:layout_marginRight="@dimen/item_country_picker_margin"/>

    <TextView
        android:id="@+id/tv_country"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center"
        android:text="country"/>

</LinearLayout>